<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adan.Formu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.adan.formu.MainChido">
            <intent-filter>
                <action android:name="android.intent.action.MainChido" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.adan.formu.Prueba"
            android:label="@string/title_activity_prueba"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>